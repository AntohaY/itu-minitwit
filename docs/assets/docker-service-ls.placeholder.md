# Placeholder: docker-service-ls.txt

Target asset: `docker-service-ls.txt`

Store sanitized Swarm service status output.

Suggested commands:

```sh
docker node ls
docker service ls
docker service ps minitwit_webserver
docker stack ps minitwit
```

Priority: Strongly useful.
